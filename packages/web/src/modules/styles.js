import { unsafeCSS } from "lit";

import bootstrapIconsCss from "bootstrap-icons/font/bootstrap-icons.css";
import bootstrapCss from "../../css/bootstrap.scss";

export const bootstrap = unsafeCSS(bootstrapCss);
export const bootstrapIcons = unsafeCSS(bootstrapIconsCss);
